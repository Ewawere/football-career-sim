/**
 * Legacy entry point — the real UI loads as:
 *   app-core.js → app-views-a.js → app-views-b.js → app-boot.js
 * See public/app.html script tags.
 * This file is intentionally minimal so GitHub always has a valid entry.
 */
console.info("[Football Career Sim] Prefer split scripts in app.html (core/views/boot).");
